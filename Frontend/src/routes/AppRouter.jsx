import { BrowserRouter, Routes, Route, Outlet } from 'react-router-dom';

const AppRouter = () => {
	return (
		<BrowserRouter>
			<Routes></Routes>
		</BrowserRouter>
	);
};

export default AppRouter;
